<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit figure</title>
</head>
<body>

  <input type="hidden" id="hidden" name="_method" value="PATCH">
  <form action="/figures/<%=@figure.id%>" method="POST">
  <h1> Edit <%= @figure.name %> </h1>
  <label for "figure_name"> Name: </label> 
    <input type="text" name="figure[name]" id="figure_name">
    
    <br></br>

    <label> associate this figure with a landmark: </label>
      <% @landmarks.each do |landmark| %>
      <label> <%= landmark.name %></label><br>
        <input type="checkbox" id="landmark_<%= landmark.id %>" name="figure[landmark_ids][]" value="<%= landmark.id %>"> 
      <% end %>  
      <br></br>
    <label for "new_landmark"> or, create a new landmark </label>
      <input type="text" name="landmark[name]" id="new_landmark">


      <br></br> 

    <label> associate this figure with a title: </label>
      <% @titles.each do |title| %>
        <label> <%= title.name %> </label><br>
        <input type="checkbox" id="title_<%= title.id %>" name="figure[title_ids][]" value="<%= title.id %>">
        <% end %>
        <br></br> 
    <label for "new_title"> or, create a new title </label>
      <input type="text" name="title[name]" id="new_title">

        <br></br>
        
      <input type="submit" value="Edit Figure">
  </form>  
</body>

</html>